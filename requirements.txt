# =============================================================================
# Whisper Project - Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
#
# IMPORTANT: PyTorch must be installed FIRST with CUDA support.
# Run this command before pip install -r requirements.txt:
#
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# =============================================================================

# --- Core Whisper Engine (CTranslate2-based, 4-8x faster than openai-whisper) ---
faster-whisper>=1.1.0
nemo_toolkit[asr] @ git+https://github.com/NVIDIA-NeMo/NeMo.git@main
nvidia-riva-client>=2.18.0

# --- Audio Capture & Processing ---
sounddevice>=0.5.1
numpy>=1.26.0

# --- Screenshot & OCR ---
mss>=9.0.1
Pillow>=10.4.0
pytesseract>=0.3.13

# --- Windows System Integration ---
pywin32>=306
keyboard>=0.13.5
pyperclip>=1.8.2
keyring>=24.0.0
pycaw>=20240210

# --- UI / Overlay ---
PyQt6>=6.7.0
PyQt6-WebEngine>=6.7.0
pyqtgraph>=0.13.7

# --- Terminal Output (colored text on Windows) ---
colorama>=0.4.6
